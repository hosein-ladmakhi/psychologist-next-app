import { Vazirmatn } from 'next/font/google';

export const vazirFont = Vazirmatn({
  display: 'swap',
  style: 'normal',
  subsets: ['latin'],
  weight: ['100', '200', '300', '400', '500'],
  preload: true,
});
